# openspec/specs

本目录**不是**全量契约真源。权威定义在 `docs/`（见 `openspec/project.md`）。

- 可选：archive 后在此按域存放**行为摘要**（从 `docs/` 抽取）。
- 新建 change 时，delta 写在 `openspec/changes/<id>/specs/`，审阅后合并进 `docs/`。

勿在此维护与 `docs/api.md` 等全文重复的副本。
