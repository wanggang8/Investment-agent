# P105 Release v0.1.1

## Why

P100-P104 completed local-source final acceptance, config/LLM compatibility, product acceptance, UX linkage fixes, and repeatable operation-linkage acceptance after the initial `v0.1.0` marker. The repository now needs a new release version and tag boundary for the validated source state.

## What Changes

- Bump the current repository release marker from `v0.1.0` to `v0.1.1`.
- Sync frontend npm metadata to `0.1.1`.
- Update release-facing materials to describe `v0.1.1` as the current source release.
- Preserve `v0.1.0` as the historical initial release marker recorded by P99.
- Run release gates, archive P105, commit, and tag `v0.1.1`.

## Scope

In scope:

- Local source version metadata and release documentation.
- Git commit and annotated tag creation for `v0.1.1`.

Out of scope:

- New product/runtime investment capability.
- Docker validation, installer validation, package refresh, physical second-machine validation, or broker/automatic trading capabilities.
