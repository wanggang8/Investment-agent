# Tasks: P8 前端体验与测试

## 1. P8.1 驾驶舱图表与关键交互

- [x] 1.1 在今日纪律页面增加图表组件，用于展示仓位、风险或证据覆盖状态。
- [x] 1.2 在持仓页面增加图表组件，用于展示仓位结构或风险状态。
- [x] 1.3 在复盘页面增加图表组件，用于展示复盘摘要。
- [x] 1.4 确保图表数据只来自 API DTO，不直接读取 SQLite、VecLite 或本地文件。
- [x] 1.5 增强证据、决策链、审计时间线的筛选和展开交互。
- [x] 1.6 对信息不足、数据过期、LLM 降级、VecLite 不可用等状态提供明确空态和错误态。
- [x] 1.7 保持用户确认区为线下动作记录，不增加一键交易或自动下单入口。
- [x] 1.8 为非显然的图表映射、状态映射或安全边界判断添加中文注释。
- [x] 1.9 执行 P8.1 验收命令：`cd web && npm run build`。

## 2. P8.2 前端测试与契约校验

- [x] 2.1 建立前端测试脚本，确保 `npm test` 可执行。
- [x] 2.2 覆盖 API client 的 `request_id`、`data`、`error` 处理。
- [x] 2.3 覆盖信息不足、冻结观察、高危、降级、错误响应等 UI 状态。
- [x] 2.4 覆盖用户确认流程，确认该流程只记录线下动作。
- [x] 2.5 覆盖规则提案最终确认流程，确认 `pending_final_confirm` 可见且不会自动应用规则。
- [x] 2.6 覆盖禁止自动交易入口的页面断言。
- [x] 2.7 确认页面字段继续符合 `docs/frontend-contract.md`。
- [x] 2.8 将前端测试命令纳入本阶段验收记录。
- [x] 2.9 执行 P8.2 验收命令：`cd web && npm run build && npm test`。

## 3. 阶段边界确认

- [x] 3.1 确认未实现 P9 `cmd/agent`、月度/季度复盘自动化或本地交付说明。
- [x] 3.2 确认未新增后端 API 契约之外的数据读取路径。
- [x] 3.3 确认未新增自动交易、一键交易、代下单或收益承诺能力。
- [x] 3.4 执行 `openspec instructions apply --change p8-frontend-experience-tests --json`，确认 artifacts 完整且 tasks 可追踪。
