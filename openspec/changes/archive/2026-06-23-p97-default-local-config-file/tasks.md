# P97 Tasks

## 1. Config Resolution

- [x] Change default config resolution to prefer `configs/config.yaml`.
- [x] Keep `configs/config.example.yaml` as fallback when no local config exists.
- [x] Add or update tests for default resolution order.

## 2. Local Secret Safety

- [x] Add `configs/config.yaml` to `.gitignore`.
- [x] Keep `configs/config.example.yaml` tracked and key-free.

## 3. Documentation And Acceptance

- [x] Update local startup/config documentation.
- [x] Generate P97 acceptance record.
- [x] Run focused config tests, backend tests, OpenSpec, P93, and diff checks.
- [x] Archive P97 after validation passes.
