# P65 Cross-Machine-Equivalent Release Repeat Acceptance

> Date: 2026-06-18
> Change: `p65-cross-machine-release-repeat-acceptance`
> Status: `passed`
> Scope: P65 candidate archive generated by the P64 package workflow, then repeated from an isolated extracted workspace.

## Summary

P65 repeated release acceptance from a local package archive rather than the active repository checkout. The repeat flow verified the archive and sidecar manifest, extracted the package under project `tmp/`, ran validation from the extracted package root, and wrote a sanitized repeat summary.

This is a cross-machine-equivalent local isolated repeat. It is not proof that a separate physical second machine has run the package.

## Package Identity

| Field | Value |
| --- | --- |
| Release label | `p65-rc` |
| Archive | `tmp/p65-release/20260618T061502Z/investment-agent-p65-rc.tar.gz` |
| Sidecar manifest | `tmp/p65-release/20260618T061502Z/release-manifest.json` |
| Package SHA-256 | `a0ae2c5c3a6e07485ee1cdd547250e41611dfe9e7fdf467d80c0440dedf47a6d` |
| Source commit | `ef2f55acfcd2ee5e96676a59014e3766282b876a` |
| Source status | `dirty` |
| Repeat summary | `tmp/p65-repeat/20260618T061511Z/repeat-summary.json` |
| `skip_install` | `false` |
| `skip_e2e` | `false` |

The `dirty` source status is expected for this active P65 implementation package. A final distribution package should be regenerated after the P65 commit from a clean working tree.

## Commands Run

```bash
bash scripts/local-release-package.sh --release-label p65-rc --output-dir tmp/p65-release
bash scripts/local-release-package.sh --verify tmp/p65-release/20260618T061502Z/investment-agent-p65-rc.tar.gz --output-dir tmp/p65-release
bash scripts/local-release-repeat-acceptance.sh --archive tmp/p65-release/20260618T061502Z/investment-agent-p65-rc.tar.gz --output-dir tmp/p65-repeat
```

## Repeat Command Matrix

| Command | Result | Duration |
| --- | --- | --- |
| `openspec validate --all --strict` | passed | 1s |
| `go test ./...` | passed | 12s |
| `npm --prefix web ci` | passed | 2s |
| `npm --prefix web test` | passed | 7s |
| `npm --prefix web run build` | passed | 3s |
| `env E2E_SERVER_PORT=18165 E2E_WEB_PORT=14265 bash scripts/e2e-smoke.sh` | passed | 30s |

## Safety And Redaction Checks

Archive listing and summary scans completed with no forbidden entries found for complete API keys, private local paths, SQLite databases, logs, traces, local config, generated frontend/test outputs, raw prompt payloads, raw vendor payloads, or UI audit assets.

The repeat script writes only under project `tmp/`, uses temporary smoke ports, and runs commands from the extracted package root. It does not write to a real user database, private configuration directory, home directory, or the active repository source tree.

## Caveats

| Item | Position |
| --- | --- |
| Physical second machine | Not performed in P65. This record covers a local isolated repeat that is equivalent for package-root and clean-workspace validation. |
| Source status | The package was generated while P65 files were active and uncommitted, so `source_status` is `dirty`. Regenerate after commit for final distribution. |
| Real providers | No public-source or LLM provider calls are made by the repeat script. Passing this repeat does not guarantee future provider availability. |
| E2E workers | The smoke script runs Playwright with one worker for release repeat stability against a single temporary local SQLite database. |

## Not Claimed

P65 does not claim remote publishing, Git tag creation, installer signing, automatic upgrade, automatic migration, automatic restore, automatic repair, real database overwrite, broker connectivity, automatic trading, one-click trading, order delegation, delegated order placement, external push, automatic confirmation, automatic rule application, login-gated sources, paid sources, authorization-gated sources, Level2 data, high-frequency data, future provider availability, or investment returns.
